import { Repository } from "typeorm";
import { User } from "../entities/user.entity";
import { Department } from "../entities/department.entity";
import { Project } from "../entities/project.entity";
import { Phase } from "../entities/phase.entity";
import { Task } from "../entities/task.entity";
import { BudgetCategory } from "../finance/entities/budget-category.entity";
import { ProjectTransaction } from "../finance/entities/project-transaction.entity";
import { ProjectSavings } from "../finance/entities/project-savings.entity";
import { BudgetAlert } from "../finance/entities/budget-alert.entity";
import { Admin } from "../entities/admin.entity";
import { Activity } from "../entities/activity.entity";
import { Report } from "../entities/report.entity";
import { Comment } from "../entities/comment.entity";
import { Complaint } from "../entities/complaint.entity";
import { SubPhase } from "../entities/sub-phase.entity";
import { Inventory } from "../entities/inventory.entity";
export declare class SeedService {
    private readonly userRepository;
    private readonly departmentRepository;
    private readonly projectRepository;
    private readonly phaseRepository;
    private readonly taskRepository;
    private readonly budgetCategoryRepository;
    private readonly transactionRepository;
    private readonly savingsRepository;
    private readonly alertRepository;
    private readonly adminRepository;
    private readonly activityRepository;
    private readonly reportRepository;
    private readonly commentRepository;
    private readonly complaintRepository;
    private readonly subPhaseRepository;
    private readonly inventoryRepository;
    constructor(userRepository: Repository<User>, departmentRepository: Repository<Department>, projectRepository: Repository<Project>, phaseRepository: Repository<Phase>, taskRepository: Repository<Task>, budgetCategoryRepository: Repository<BudgetCategory>, transactionRepository: Repository<ProjectTransaction>, savingsRepository: Repository<ProjectSavings>, alertRepository: Repository<BudgetAlert>, adminRepository: Repository<Admin>, activityRepository: Repository<Activity>, reportRepository: Repository<Report>, commentRepository: Repository<Comment>, complaintRepository: Repository<Complaint>, subPhaseRepository: Repository<SubPhase>, inventoryRepository: Repository<Inventory>);
    seed(): Promise<void>;
    private seedDepartments;
    private seedUsers;
    private seedAdmins;
    private seedProjects;
    private seedPhases;
    private seedSubPhasesForPhase;
    private seedTasks;
    private seedFinancialData;
    private createTransactionsForCategory;
    private createSavingsRecords;
    private createBudgetAlerts;
    private seedActivities;
    private seedReports;
    private seedComments;
    private seedComplaints;
    private getRandomTransactionDescription;
    private getSavingsReason;
    private getMimeType;
    private seedInventoryItems;
}
export declare class SeedCommand {
    private readonly seedService;
    constructor(seedService: SeedService);
    run(): Promise<void>;
}
